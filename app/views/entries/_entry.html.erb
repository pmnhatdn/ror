<li>
  <span class="content"><%= entry.content %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(entry.created_at) %> ago.
  </span><%= link_to 'Delete', entry, method: :delete, data: { confirm: 'Are you sure?' } %>
</li>